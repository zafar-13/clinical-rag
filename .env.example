# Clinical RAG API - Environment Variables
# Copy this to .env and fill in your values

# Google Cloud Project (for Secret Manager)
GOOGLE_PROJECT_ID=llmops-rag-project

# Option 1: Use these if NOT using GCP Secret Manager
# GEMINI_API_KEY=your-gemini-api-key
# MILVUS_URI=https://your-milvus-uri.cloud.zilliz.com
# MILVUS_TOKEN=your-milvus-token

# Option 2: If using GCP Secret Manager, just set:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/credentials.json

# Model Configuration (optional, these are defaults)
# EMBEDDING_MODEL_ID=models/text-embedding-004
# GENERATION_MODEL_ID=models/gemini-2.5-flash

# Chunking Configuration (optional)
# CHUNK_SIZE=1000
# CHUNK_OVERLAP=100
